#ifndef _SECRETS
#define _SECRETS
static char *host = (char*)"tvboy";
static char *ssid = (char*)"name";			 // To avoid having to enter it manually
static char *password = (char*)"password"; // To avoid having to enter it manually

#endif
