#include "Lagunita.hpp"

/* The game instance. */
Lagunita game;


/* To save some memory. */
//ARDUBOY_NO_USB

/* Initial setup */
void inogamesetup() { Serial.begin(115200); game.init(); }

/* Game loop */
void inogameloop() { game.loop(); }
